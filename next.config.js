/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable Cache Components (replaces experimental PPR flags in Next.js 16)
  cacheComponents: true,
}

module.exports = nextConfig


